# .github/workflows/release.yml
#
# Triggered manually from the GitHub Actions UI ("Run workflow" button).
# Steps:
#   1. Build
#   3. Extract the changelog for the current version from CHANGELOG.md
#   4. Create a GitHub Release with both JARs attached
#   5. Publish to Modrinth

name: Release

on:
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Dry run (build & parse changelog but do NOT publish)"
        type: boolean
        default: false

permissions:
  contents: write  # needed to create the GitHub Release

jobs:
  release:
    name: Build & Release
    runs-on: ubuntu-latest

    steps:
      - name: Checkout (target ref)
        uses: actions/checkout@v4

      - name: Set up JDK 21 (Temurin)
        uses: actions/setup-java@v4
        with:
          java-version: "21"
          distribution: temurin
          cache: gradle

      - name: Grant execute permission for Gradle wrapper
        run: chmod +x ./gradlew

      # ── Read version from gradle.properties ──────────────────────────────────
      - name: Read mod version
        id: version
        run: |
          MAJOR=$(grep -m1 '^version_major' gradle.properties | cut -d= -f2 | tr -d ' \r')
          MINOR=$(grep -m1 '^version_minor' gradle.properties | cut -d= -f2 | tr -d ' \r')
          PATCH=$(grep -m1 '^version_patch' gradle.properties | cut -d= -f2 | tr -d ' \r')
          MC=$(grep -m1 '^minecraft_version=' gradle.properties | cut -d= -f2 | tr -d ' \r')
          MOD_ID=$(grep -m1 '^mod_id=' gradle.properties | cut -d= -f2 | tr -d ' \r')
          VERSION="${MAJOR}.${MINOR}.${PATCH}"
          
          echo "DEBUG: MC='${MC}' VERSION='${VERSION}' MOD_ID='${MOD_ID}'"
          
          {
            echo "version=${VERSION}"
            echo "mc_version=${MC}"
            echo "mod_id=${MOD_ID}"
            echo "tag=mc${MC}-v${VERSION}"
            echo "jar_base=${MOD_ID}-neoforge-${MC}"
          } >> "$GITHUB_OUTPUT"

      # ── Extract changelog for this version ───────────────────────────────────
      - name: Extract changelog entry
        id: changelog
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          CHANGELOG_FILE="$GITHUB_WORKSPACE/CHANGELOG.MD"
          
          ENTRY=$(awk "
            /^# .*${VERSION}/ { found=1; next }
            found && /^## /   { next }
            found && /^# /    { exit }
            found && /^---/   { exit }
            found             { print }
          " "${CHANGELOG_FILE}")
          
          if [ -z "$ENTRY" ]; then
            echo "::warning::No changelog entry found for version ${VERSION}"
            ENTRY="See CHANGELOG.md for details."
          fi
          
          printf '%s\n' "$ENTRY" > /tmp/changelog_entry.md
          echo "Changelog entry:"
          cat /tmp/changelog_entry.md

      # ── Build Mod JAR ────────────────────────────────────────────────────
      - name: Run data generation
        run: >
          ./gradlew :neoforge:runData
          --stacktrace
          -Porg.gradle.jvmargs="-Xmx2g"
        timeout-minutes: 10
        env:
          CI: true

      - name: Build jar
        run: ./gradlew :neoforge:build --stacktrace

      # ── GitHub Release ────────────────────────────────────────────────────────────
      - name: Create GitHub Release
        if: ${{ !inputs.dry_run }}
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.version.outputs.tag }}
          name: "Create: Webdisc ${{ steps.version.outputs.version }}"
          body_path: /tmp/changelog_entry.md
          draft: false
          prerelease: false
          files: |
            neoforge/build/libs/${{ steps.version.outputs.jar_base }}-${{ steps.version.outputs.version }}.jar

      # ── Publish to Modrinth ───────────────────────────────────────────────────────
      - name: Publish to Modrinth
        if: ${{ !inputs.dry_run }}
        uses: Kir-Antipov/mc-publish@v3.3
        with:
          modrinth-id: ${{ secrets.MODRINTH_PROJECT_ID || steps.version.outputs.mod_id }}
          modrinth-token: ${{ secrets.MODRINTH_TOKEN }}
          modrinth-featured: true

          name: "Create: Webdisc ${{ steps.version.outputs.version }} for Minecraft ${{ steps.version.outputs.mc_version }}"
          version: ${{ steps.version.outputs.version }}
          version-type: release
          changelog-file: /tmp/changelog_entry.md

          files: neoforge/build/libs/${{ steps.version.outputs.jar_base }}-${{ steps.version.outputs.version }}.jar

          game-versions: ${{ steps.version.outputs.mc_version }}
          loaders: neoforge
          dependencies: |
            create | depends | *
            kotlin-for-forge | depends | *
            jei | optional | *

      # ── Summary ──────────────────────────────────────────────────────────────
      - name: Job summary
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          DRY="${{ inputs.dry_run }}"
          {
            echo "## Release $VERSION"
            if [ "$DRY" = "true" ]; then
              echo "> **Dry run** — nothing was published."
            else
              echo "✅ Published to GitHub Releases and Modrinth."
            fi
            echo ""
            echo "### Changelog"
            cat /tmp/changelog_entry.md
          } >> "$GITHUB_STEP_SUMMARY"
